<Window x:Class="PixBeat5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="PixBeat 5.0 - Simple Pixel Art Music Video Generator"
        Height="700" Width="900"
        MinHeight="600" MinWidth="800"
        ui:WindowHelper.UseModernWindowStyle="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,30">
            <TextBlock Text="🎵" FontSize="40" VerticalAlignment="Center" Margin="0,0,15,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="PixBeat 5.0" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Simple Pixel Art Music Video Generator" 
                          FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </StackPanel>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Step 1: Audio File -->
                <Border Background="{StaticResource CardBackground}"
                        BorderBrush="{StaticResource CardBorder}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Step 1: Select Audio File" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                    Text="{Binding AudioFilePath}"
                                    IsReadOnly="True"/>

                            <Button Grid.Column="1"
                                   Content="Browse Audio File"
                                   Command="{Binding SelectAudioFileCommand}"
                                   Margin="10,0,0,0"/>
                        </Grid>

                        <!-- Audio Analysis Results - Simple visibility -->
                        <Border Background="{StaticResource ChromeLowBrush}"
                               CornerRadius="6"
                               Padding="15"
                               Margin="0,15,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasAnalysis}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="BPM" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Tempo, StringFormat={}{0:F1}}" 
                                              FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Genre" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Genre}" 
                                              FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Key" FontSize="12" Foreground="Gray"/>
                                    <TextBlock FontSize="16" FontWeight="SemiBold">
                                        <Run Text="{Binding Key}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding Mode}"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="Duration" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Duration, StringFormat={}{0:mm\\:ss}}" 
                                              FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Step 2: Template Selection -->
                <Border Background="{StaticResource CardBackground}"
                        BorderBrush="{StaticResource CardBorder}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Step 2: Choose Template" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <!-- Simple RadioButton group -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <RadioButton GroupName="Templates" 
                                       Content="Pixel Runner"
                                       IsChecked="True"
                                       Command="{Binding SelectTemplateCommand}"
                                       CommandParameter="pixel_runner"
                                       Margin="10"/>

                            <RadioButton GroupName="Templates" 
                                       Content="Audio Equalizer"
                                       Command="{Binding SelectTemplateCommand}"
                                       CommandParameter="equalizer"
                                       Margin="10"/>

                            <RadioButton GroupName="Templates" 
                                       Content="Waveform Visualizer"
                                       Command="{Binding SelectTemplateCommand}"
                                       CommandParameter="waveform"
                                       Margin="10"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Step 3: Video Settings -->
                <Border Background="{StaticResource CardBackground}"
                        BorderBrush="{StaticResource CardBorder}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                    <StackPanel>
                        <TextBlock Text="Step 3: Video Settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Text="Quality" FontSize="12" Margin="0,0,0,5"/>
                                <ComboBox SelectedIndex="1">
                                    <ComboBoxItem Content="Draft"/>
                                    <ComboBoxItem Content="Standard"/>
                                    <ComboBoxItem Content="High"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Watermark Text" FontSize="12" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding WatermarkText}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="2"
                    Height="6"
                    Value="{Binding ProgressValue}"
                    Maximum="100"
                    Margin="0,20,0,15"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Status and Action Bar -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ProgressText}" 
                          FontSize="12" 
                          Foreground="Gray"
                          Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <!-- Reset Project Button -->
                <Button Content="Reset Project" 
                       Command="{Binding ResetProjectCommand}"
                       Margin="0,0,10,0">
                    <Button.IsEnabled>
                        <Binding Path="IsProcessing">
                            <Binding.Converter>
                                <BooleanToVisibilityConverter/>
                            </Binding.Converter>
                            <Binding.ConverterParameter>
                                <sys:Boolean xmlns:sys="clr-namespace:System;assembly=mscorlib">True</sys:Boolean>
                            </Binding.ConverterParameter>
                        </Binding>
                    </Button.IsEnabled>
                </Button>

                <!-- Open Output Folder Button -->
                <Button Content="Open Output Folder" 
                       Command="{Binding OpenOutputFolderCommand}"
                       Margin="0,0,10,0"
                       Visibility="{Binding HasOutputFile, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Generate Video Button -->
                <Button Content="🎬 Generate Video!" 
                       Command="{Binding GenerateVideoCommand}"
                       Background="{StaticResource AccentBrush}"
                       Foreground="White"
                       BorderBrush="{StaticResource AccentBrush}"
                       Padding="16,8"
                       FontWeight="SemiBold"
                       Width="150" Height="40"
                       FontSize="14"
                       IsEnabled="{Binding CanRender}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>